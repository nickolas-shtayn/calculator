import { calculate } from "./calculator-model";

describe("Calculate", () => {
    test("addition", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()
    });

    test("subtraction", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe() 
    });

    test("multiplication", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()        
    });

    test("division", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()        
    });

    test("division by 0", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()        
    });

    test("modulo", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()        
    });

    test("modulo of 0", () => {
        firstNumber = ;
        secondNumber = ;
        operation = ;

        const result = calculate();

        expect(result).toBe()        
    });
});